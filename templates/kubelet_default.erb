<%- unless @labels.empty? -%>
# Warning: Alpha feature not supported in /etc/kubernetes/config.yaml yet
KUBELET_EXTRA_ARGS+="--node-labels=<%= @labels.sort.map{|k,v| "#{k}=#{v}"}.join(',') -%>"
<% end -%>
